spring:
  datasource:
    url: jdbc:mysql://localhost:3306/haui_clinic?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: zxczxc123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    cache: false

server:
  port: 8080

logging:
  level:
    org.springframework.security: DEBUG
